pipeline {
    agent any

    stages {
        stage('Check Out') {
            steps {
                checkout scm
                echo 'Success : Git Check Out'
            }
        }

        stage('Build') {
            steps {
                bat './gradlew clean build'
                echo 'Success : build'
            }
        }

        stage('Test') {
            steps {
                bat './gradlew test'
                echo 'Success : test'
            }
        }
    }
}
